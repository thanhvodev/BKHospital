<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml" xmlns:viewmodels="clr-namespace:AppActivityIndicator.ViewModels" x:DataType="viewmodels:PaymentViewModel"
             x:Class="AppActivityIndicator.Views.PaymentPage"
             Title="{Binding Title}">
    <Shell.BackButtonBehavior>
        <BackButtonBehavior Command="{Binding BackToPayFeeCommand}" />
    </Shell.BackButtonBehavior>
    <ContentPage.Content>
        <StackLayout>
            <Label Text="Thời gian"
                HorizontalOptions="CenterAndExpand" />
            <Label Text="{Binding StartTime}"
                HorizontalOptions="CenterAndExpand" />
        </StackLayout>
    </ContentPage.Content>
</ContentPage>